@model IEnumerable<WebApplication1.Models.ViewModels.TipoDepartamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoDepartamentoNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoDepartamentoDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoDepartamentoNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoDepartamentoDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoDepartamentoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoDepartamentoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoDepartamentoID })
        </td>
    </tr>
}

</table>
